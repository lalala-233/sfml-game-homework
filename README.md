# Snake Game

A different flavor of classic game.

I build this game statically with x86_64-w64-mingw32-gcc. But I don't build it statically by normal gcc.

So linux users should build it yourself :P

```bash
> cmake -B build/linux && cmake --build build/linux
```
